"""ckit — the content-kit engine: serve, lint, navigate, scaffold and annotate a content tree.

Installed once (see install-engine.sh) and run from inside any repo that vendored the
kit's shell. The repo declares which engine version it was checked against in `lab.json`
(`"ckit": "<version>"`); `ckit check` fails loud on a mismatch.
"""

__version__ = "0.1.0"
